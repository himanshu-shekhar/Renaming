This is a python code for the users who usually add a nick at the end of their files.
In windows this file can be copy pasted in the sendTo folder for easily using it.
One can directly select all the files which need to be renamed and just open it with this program.